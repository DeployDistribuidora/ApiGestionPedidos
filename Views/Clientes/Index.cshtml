@model Front_End_Gestion_Pedidos.Models.ViewModel.ClienteViewModel

@{
    ViewData["Title"] = "Gestión de Clientes";
    string searchTerm = ViewBag.SearchTerm;
}

<div class="pagetitle">
    <h1>Gestión de Clientes</h1>
</div>

<section class="section">
    <div class="row">
        <!-- Alertas -->
        <div class="col-12">
            @if (ViewBag.Alerta != null)
            {
                <div class="alert alert-warning alert-dismissible fade show" role="alert">
                    <i class="bi bi-exclamation-circle"></i> @ViewBag.Alerta
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            }

            @if (ViewBag.Error != null)
            {
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <i class="bi bi-exclamation-triangle"></i> @ViewBag.Error
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            }
        </div>

        <!-- Filtro de búsqueda -->
        <div class="col-4">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Filtro de Búsqueda</h5>
                    <form method="get" asp-action="BuscarClientes" class="row g-3">
                        <div class="col-md-6">
                            <input type="text" name="searchTerm" value="@ViewBag.SearchTerm" class="form-control form-control-sm" placeholder="Buscar clientes..." />
                        </div>
                        <div class="col-auto">
                            <button type="submit" class="btn btn-primary btn-sm">Buscar</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <!-- Tabla de clientes -->
        <div class="col-10">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Clientes</h5>
                    <div class="table-responsive">
                        <table class="table table-hover table-sm align-middle text-nowrap">
                            <thead class="table-primary">
                                <tr>
                                    <th>NroCliente</th>
                                    <th>Nombre</th>
                                    <th>Dirección</th>
                                    <th>Teléfono</th>
                                    <th>Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model.Clientes != null && Model.Clientes.Count > 0)
                                {
                                    foreach (var cliente in Model.Clientes)
                                    {
                                        <tr>
                                            <td>@cliente.NroCliente</td>
                                            <td>@cliente.NombreCliente</td>
                                            <td>@cliente.DirCliente</td>
                                            <td>@cliente.TelefCliente</td>
                                            <td>
                                                <form method="post" asp-action="VerDatosContacto" class="d-inline">
                                                    <input type="hidden" name="clienteId" value="@cliente.NroCliente" />
                                                    <button type="submit" class="btn btn-info btn-sm">Ver</button>
                                                </form>
                                            </td>
                                        </tr>
                                    }
                                }
                                else
                                {
                                    <tr>
                                        <td colspan="5" class="text-center text-muted">No se encontraron resultados</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>



@* @model Front_End_Gestion_Pedidos.Models.ViewModel.ClienteViewModel


@{
    ViewData["Title"] = "Gestión de Clientes";
    string searchTerm = ViewBag.SearchTerm;
}

<div class="container mt-4">
    <!-- Mostrar alertas si existen -->
    @if (ViewBag.Alerta != null)
    {
        <div class="alert alert-warning alert-dismissible fade show" role="alert">
            <i class="bi bi-exclamation-circle"></i> @ViewBag.Alerta
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    @if (ViewBag.Error != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="bi bi-exclamation-triangle"></i> @ViewBag.Error
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <!-- Encabezado de la página -->
    <h2 class="text-primary text-center mb-4">Gestión de Clientes</h2>

    <!-- Filtro de búsqueda -->
    <form method="get" asp-action="BuscarClientes" class="row g-2 align-items-center mb-4">
        <div class="col-sm-9 col-md-6">
            <input type="text" name="searchTerm" value="@ViewBag.SearchTerm" class="form-control form-control-sm" placeholder="Buscar clientes..." />
        </div>
        <div class="col-auto">
            <button type="submit" class="btn btn-primary btn-sm">Buscar</button>
        </div>
    </form>

    <!-- Tabla de clientes -->
    <div class="table-responsive">
        <table class="table table-hover table-sm align-middle text-nowrap">
            <thead class="table-primary">
                <tr>
                    <th>NroCliente</th>
                    <th>Nombre</th>
                    <th>Dirección</th>
                    <th>Teléfono</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Clientes != null && Model.Clientes.Count > 0)
                {
                    foreach (var cliente in Model.Clientes)
                    {
                        <tr>
                            <td>@cliente.NroCliente</td>
                            <td>@cliente.NombreCliente</td>
                            <td>@cliente.DirCliente</td>
                            <td>@cliente.TelefCliente</td>
                            <td>
                                <form method="post" asp-action="VerDatosContacto" class="d-inline">
                                    <input type="hidden" name="clienteId" value="@cliente.NroCliente" />
                                    <button type="submit" class="btn btn-info btn-sm">Ver</button>
                                </form>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="5" class="text-center text-muted">No se encontraron resultados</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>


 *@